<?php
// //$serverName = "serverName\\sqlexpress"; 
// $serverName = "127.0.0.1"; //serverName\instanceName
// $serverName = "HRNBK01202\SQLEXPRESS"; //Hostname/IP,...12KPRVPEIC01
// $serverName = "127.0.0.1"; //Hostname/IP,...12KPRVPEIC01
// //$connectionString="data source=DESKTOP-S1IGBM3\SQLEXPRESS;initial catalog=FENIXBD;integrated security=True;MultipleActiveResultSets=True;App=EntityFramework" 
// $connectionOptions = array(
//     "Database" => "procesos_ic",
//     "Uid" => "sa",
//     "PWD" => "12345678"
// );

// //Establishes the connection
// $conn = sqlsrv_connect($serverName, $connectionOptions);

// if( $conn === false ) {
//     die( print_r( sqlsrv_errors(), true)); //See why it fails
// } 

class Conexion {

     static public function conectar(){
          
          $host     = "DESKTOP-C44HMFF\SQLEXPRESS";
          $dbname   = "procesos_ic";
          $usuario  = "sa";
          $password = "12345678";
          
          //$link = new PDO("mysql:host=".$host.";dbname=".$dbname,$usuario , $password);
          $link = new PDO('sqlsrv:Server='.$host.';Database='.$dbname, $usuario, $password);

        //   $link ->exec("SET names utf8;");

          $link->setAttribute(PDO::ATTR_ERRMODE, PDO::ERRMODE_EXCEPTION);
          //$link->setAttribute(PDO::ATTR_EMULATE_PREPARES, false);

     
          return $link;
     }			

}
